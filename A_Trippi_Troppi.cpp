#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    cin.ignore();

    while (t--)
    {
        string a, b, c;
        cin >> a >> b >> c;
        char x = a[0];
        char y = b[0];
        char z = c[0];
        cout << x << y << z << endl;
    }
    return 0;
}